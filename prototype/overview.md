# Prototype Overview

本目录当前是预留目录，不是仓库当前主实现入口。

当前真正可运行的原型主要在：

- `experiments/scripts/`
- `skills/`

本目录保留的原因是，为后续更系统化的实现留出明确落点，例如：

- `classifier/`
- `policy/`
- `retrieval_guard/`
- `audit/`
- `sandbox/`

后续如果继续做内核级集成或把外挂式脚本收敛成稳定模块，再把实现逐步迁入这里。
